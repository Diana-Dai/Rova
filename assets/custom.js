window.onload = function () {
    var elem = document.querySelector('.collection-list__container ul');
    var flkty = new Flickity( elem, {
    // options
    cellAlign: 'left'
    // contain: true
    });
  }